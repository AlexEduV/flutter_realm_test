### Widget book exports

The widget book library has an option for the automatic generation of the folders/components, but I use a bit of a
custom builder mocks in a few cases, so it probably will not work.